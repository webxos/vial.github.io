FROM python:3.11-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8000 27017

CMD ["sh", "-c", "mongod & uvicorn server.mcp_server:app --host 0.0.0.0 --port 8000"]
